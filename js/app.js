document.addEventListener("DOMContentLoaded", () => {
    initNavbar();

    initSmoothScroll();

    initBackToTop();

    initContactForm();

    initExperienceModal();

    renderHeroProfile();

    renderHeroStats();

    renderProjects();

    initProjectFilters();

    initProjectSearch();

    initCounters();

});